meta {
  name: Login as bob and save token to env
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "calamardo@test.com",
    "password": "Pa$$w0rd"
  }
}

tests {
  test("Should be able to login", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  const data = res.body;
  bru.setVar('otherToken', data.token);
}

settings {
  encodeUrl: true
}
