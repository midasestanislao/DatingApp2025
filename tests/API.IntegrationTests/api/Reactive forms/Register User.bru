meta {
  name: Register User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/account/register
  body: json
  auth: inherit
}

body:json {
  {
      "gender": "male",
      "email": "integrationTest@test.com",
      "displayName": "Bob",
      "dateOfBirth": "1985-01-23",
      "city": "Bikini",
      "country": "Bottom",
      "password": "Pa$$w0rd"
  }
}

tests {
  test("Should be able to login", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  const data = res.body;
  bru.setVar('registrationToken', data.token);
}

settings {
  encodeUrl: true
}
