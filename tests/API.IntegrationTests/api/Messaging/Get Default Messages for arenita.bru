meta {
  name: Get Default Messages for arenita
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/messages
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "container": "inbox"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
