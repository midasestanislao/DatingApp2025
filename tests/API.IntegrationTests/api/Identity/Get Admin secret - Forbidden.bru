meta {
  name: Get Admin secret - Forbidden
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/error/admin-secret
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
