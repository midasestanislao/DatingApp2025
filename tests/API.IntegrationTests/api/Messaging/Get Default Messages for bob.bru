meta {
  name: Get Default Messages for bob
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/messages
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{otherToken}}
}

body:json {
  {
    "container": "inbox"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
