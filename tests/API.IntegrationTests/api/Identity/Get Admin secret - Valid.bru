meta {
  name: Get Admin secret - Valid
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/error/admin-secret
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
