meta {
  name: Login as patricio and save token to env copy
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "patricio@test.com",
    "password": "Pa$$w0rd"
  }
}

tests {
  test("Should be able to login", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  const data = res.body;
  bru.setVar('patricioToken', data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
